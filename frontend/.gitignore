node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# Nilai VITE_* bersifat publik (ikut ter-bundle ke browser), jadi .env.production
# sengaja di-track agar ikut git → otomatis ada saat deploy/build di GCP.
!.env.production

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
logs
*.log
npm-debug.log*
.task.md
